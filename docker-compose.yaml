version: '3.8'

services:
  acorre:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - pb_data:/app/pb/pb_data
    restart: always

volumes:
  pb_data:
